create view gold.dim_appointment as
select 
   row_number() over(order by appointment_id ) as appointment_key,
   appointment_id ,
   doctor_id  ,
   patient_id  ,
   reason_for_visit,
   status,
   appointment_date,
   appointment_time
from silver.appointments

select * from gold.dim_appointment

